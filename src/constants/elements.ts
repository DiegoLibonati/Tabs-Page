export const btnsTab = document.querySelectorAll(
  ".tabs_container_text_buttons_button"
) as NodeList;

export const tabText = document.getElementById(
  "tab-text"
) as HTMLParagraphElement;
export const tabImage = document.getElementById(
  "tab-image"
) as HTMLImageElement;
